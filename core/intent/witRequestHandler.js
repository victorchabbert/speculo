const defaultRequestHandler = require('./defaultRequestHandler');

/**
 * Default intent request handler
 *
 * @param request
 * @param reply
 */
module.exports = function (request, reply) {

  //TODO ADAPT THE OBJECT

  defaultRequestHandler(request, reply);
};
